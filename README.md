# pygit

Pygit is a basic python module foe controlling git via python

# Installing

Just download `pygit.py` and place it in your programs path.

# Dependecies

This module needs a version of git to be installed on your system.
